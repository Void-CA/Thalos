[package]
name = "thalos_core"
version = "0.1.0"
edition = "2024"

[dependencies]
nalgebra = "0.34.2"
rand = "0.8"
thiserror = "2"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "workspace_sampling"
harness = false

[[bench]]
name = "reachability"
harness = false
