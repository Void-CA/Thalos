[package]
name = "thalos-document"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
thalos_core = { path = "../thalos-core" }
thalos_semantic = { path = "../thalos-semantic" }

[dev-dependencies]
serde_json = "1"
