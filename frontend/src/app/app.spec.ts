/**
 * NOTE: These tests require vitest + @analogjs/vitest-angular to execute.
 * The project has no test runner configured for frontend yet.
 */

// ── Test plan ──
//
// Analysis is no longer a routed perspective — it's a panel inside PlanningWorkspace.
// There is no redirect guard in App. No test needed.
