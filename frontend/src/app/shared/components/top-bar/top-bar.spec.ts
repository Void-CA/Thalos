/**
 * NOTE: These tests require vitest + @analogjs/vitest-angular to execute.
 * The project has no test runner configured for frontend yet.
 */

// ── Test plan ──
//
// TopBar now renders a fixed list: ['robot', 'planning', 'execution', 'knowledge', 'sessions'].
// Analysis is NOT in the TopBar — it's a panel inside PlanningWorkspace.
// No conditional filtering is needed.
