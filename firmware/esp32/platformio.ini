; Thalos ESP32 Execution Backend
; PlatformIO build configuration
; Target: ESP32-S3 with Arduino framework

[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200

; Build flags
build_flags =
    -Os
    -DCORE_DEBUG_LEVEL=0

; Serial monitor settings
monitor_filters =
    esp32_exception_decoder
