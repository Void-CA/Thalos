// Analysis — plan-quality analysis data/content module.
// Store + API + components live here; the UI renders as a section inside the
// Planning workspace (slice 6 — analysis is not a registered workspace).
